EXPLAIN SELECT COUNT(*)
        FROM READERS
        WHERE FIRSTNAME = 'John';

CREATE INDEX BOOK_INDEX ON BOOKS (TITLE);
CREATE INDEX READER_INDEX ON READERS (FIRSTNAME, LASTNAME);

EXPLAIN SELECT COUNT(*)
        FROM READERS
        WHERE FIRSTNAME = 'John';